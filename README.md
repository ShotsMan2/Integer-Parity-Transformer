# Integer-Parity-Transformer
A C-based data transformation utility that traverses an integer array, detects elements with even parity, and converts them to their immediate odd successors via in-place modification.
